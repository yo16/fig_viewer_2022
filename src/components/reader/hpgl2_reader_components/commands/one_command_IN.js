'use strict'
/* HPGL2コマンド IN, Initialize
*/

const OneCommandBase = require('./one_command_base')

class OneCommandIN extends OneCommandBase{
    constructor(options){
        super(options)
    }
    
}

module.exports = OneCommandIN
